{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
  },
  "postCreateCommand": {
    "sudo apt-get update && sudo apt-get install -y ghostscript":
    "mkdir -p /root/.vscode-server/data/Machine && cp /workspaces/your-repo/.devcontainer/keybindings.json /root/.vscode-server/data/Machine/"
  }
}
